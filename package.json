{
  "name": "the-akin-project-news-pipeline",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Fetches RSS feeds and generates news.json for the News page.",
  "scripts": {
    "fetch-news": "node scripts/fetch-news.mjs"
  },
  "dependencies": {
    "fast-xml-parser": "^4.5.0"
  }
}
